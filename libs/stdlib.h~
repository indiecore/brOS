#ifndef __STDLIB
#define __STDLIB


int atoi(const char*);
void itoa(int, char *, unsigned int, unsigned int);
int abs(int n);
int stime();
unsigned char inportb (unsigned short _port);
void outportb (unsigned short _port, unsigned char _data);
#endif
